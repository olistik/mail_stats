module StatsPageSteps
  step "I go to the stats page" do
    pending("TBD")
    fail
  end

  step "the e-mails sent counter is :counter" do |counter|
    pending("TBD")
    fail
  end

  step "the clicks counter is :counter" do |counter|
    pending("TBD")
    fail
  end

  step "the e-mails opened counter is :counter" do |counter|
    pending("TBD")
    fail
  end
end

RSpec.configure do |config|
  config.include StatsPageSteps
end
